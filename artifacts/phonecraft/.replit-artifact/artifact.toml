kind = "web"
previewPath = "/"
title = "PhoneCraft"
version = "1.0.0"
id = "artifacts/phonecraft"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24470

[services.development]
run = "pnpm --filter @workspace/phonecraft run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/phonecraft", "run", "build" ]
publicDir = "artifacts/phonecraft/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24470"
BASE_PATH = "/"
